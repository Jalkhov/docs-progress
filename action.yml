name: "Docspro"
author: "Jalkhov"
description: "Get translation pogress from docs"
inputs:
  path:
    description: "Path to translation folder"
    required: false
    default: "trans/sample/**/**.po"
outputs:
  progress:
    description: "Output from the action"
runs:
  using: "docker"
  image: "Dockerfile"
